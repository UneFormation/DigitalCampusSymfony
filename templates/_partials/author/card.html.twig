<div class="col">
    <div class="card shadow-sm">
        <div class="card-body">
            <h4 class="card-title text-truncate">{{ author.firstname }} {{ author.lastname }}</h4>
            <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                    <a href="{{ path('author-bySlug', {slug: author.slug}) }}" class="btn btn-sm btn-outline-secondary">Voir les articles</a>
                </div>
            </div>
        </div>
    </div>
</div>